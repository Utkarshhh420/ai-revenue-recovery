# Optional: add your Anthropic API key to have Claude generate the
# reminder messages instead of the built-in templates.
# If left unset, the app automatically falls back to templates.
ANTHROPIC_API_KEY=
